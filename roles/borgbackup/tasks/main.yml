# Classic (package-based) host: install everything directly on the host.
# The packages and run-borg script are shared with the container build
# (see build-container.yml); units.yml is classic-only.
- import_tasks: packages.yml
- import_tasks: script.yml
- import_tasks: units.yml
